For the final project, I also built on the original application by adding exponentiation as a new feature. What I liked about this part was that it forced me to think about the full stack again instead of only changing one file. I had to update the model logic, schema validation, front-end behavior, and tests, which reinforced how connected all parts of the application are. It was a good reminder that even a small feature still has to be carried through the entire system if it is going to be implemented correctly.

This project furthered getting me out of my comfort zone by building an additional feature and trying not to overcopmplicate the application and break things. I simply cloned my Module 14 repository and added an additional feature including the profile one I had also just added. This project was extrememly useful and it enforced patience when debugging and also keeping in mind route design, validation, authentication, and how to make the feature fit with the rest of the app.